// describe('Chai Test', () => {
//   it('checks if chai is available and works', () => {
//     const value = 5;
//     // `to` 메서드가 제대로 작동하는지 확인
//     expect(value).to.equal(5);  // Chai의 `expect`를 이용한 테스트
//     expect(value).to.a('number');  // Chai의 `to.be`를 이용한 타입 검사
//   });
// });